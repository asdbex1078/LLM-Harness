# 迁移报告 2026-09-10

- 源：`/Users/moka/IdeaProjects/knowage`
- 目标：`/Users/moka/IdeaProjects/knowage-v2`
- 节点：62 个；新建 stub：0 个；边：194 条（方向翻转 16 条，去重丢弃 42 条）

## 边按族分布

- 结构：90
- 依赖：73
- 弱关联：14
- 演化：9
- 对照：8

## 未映射的旧类型（原样保留，归弱关联族，需人工决定）

- 无

## 新建的 stub

- 无

## 去重丢弃的边

- 二进制与香农定理 支撑 冯诺依曼体系（已有 冯诺依曼体系 依赖 二进制与香农定理）
- 冯诺依曼体系 源自 图灵机（已有 图灵机 演化为 冯诺依曼体系）
- 哈佛架构 源自 图灵机（已有 图灵机 演化为 哈佛架构）
- 哈佛架构 对比 冯诺依曼体系（对称边保留在 冯诺依曼体系）
- CPU 被控制 ISA（已有 ISA 控制 CPU）
- EPROM 属于 内存（已有 内存 包含 EPROM）
- IO接口 属于 输入输出（已有 输入输出 包含 IO接口）
- RAM 属于 内存（已有 内存 包含 RAM）
- ROM 属于 内存（已有 内存 包含 ROM）
- 专用寄存器 属于 寄存器（已有 寄存器 包含 专用寄存器）
- 专用寄存器 支撑 栈帧（已有 栈帧 依赖 专用寄存器）
- 内存 属于 计算机硬件（已有 计算机硬件 包含 内存）
- 内存地址 支撑 程序的内存布局（已有 程序的内存布局 依赖 内存地址）
- 存储器层次结构 属于 计算机组成原理（已有 计算机组成原理 包含 存储器层次结构）
- 总线 相关 CPU（对称边保留在 CPU）
- 总线 相关 内存（对称边保留在 内存）
- 总线 属于 计算机组成原理（已有 计算机组成原理 包含 总线）
- 控制器 被控制 ISA（已有 ISA 控制 控制器）
- 硬盘 属于 计算机硬件（已有 计算机硬件 包含 硬盘）
- 输入输出 属于 计算机硬件（已有 计算机硬件 包含 输入输出）
- 通用寄存器 属于 寄存器（已有 寄存器 包含 通用寄存器）
- CISC 属于 ISA（已有 ISA 包含 CISC）
- RISC 属于 ISA（已有 ISA 包含 RISC）
- RISC 对比 CISC（对称边保留在 CISC）
- 汇编器 属于 编译器后端（已有 编译器后端 包含 汇编器）
- 代码段 属于 程序（已有 程序 包含 代码段）
- 堆内存 属于 程序（已有 程序 包含 堆内存）
- 数据段 属于 程序（已有 程序 包含 数据段）
- 栈内存 属于 程序（已有 程序 包含 栈内存）
- 栈内存 对比 堆内存（对称边保留在 堆内存）
- 程序的内存布局 属于 内存（已有 内存 包含 程序的内存布局）
- 程序的内存布局 产出自 汇编器（已有 汇编器 产出 程序的内存布局）
- 程序的内存布局 相关 CPU实模式与平坦模式（对称边保留在 CPU实模式与平坦模式）
- IR 产出自 编译器前端（已有 编译器前端 产出 IR）
- LLVM 对比 GCC（对称边保留在 GCC）
- OS 支撑 JVM（已有 JVM 依赖 OS）
- 字节码 产出自 JVM语言（已有 JVM语言 产出 字节码）
- 编译器 对比 JIT（对称边保留在 JIT）
- 编译器前端 属于 编译原理（已有 编译原理 包含 编译器前端）
- 编译器后端 属于 编译原理（已有 编译原理 包含 编译器后端）
- 解释器 相关 JIT（对称边保留在 JIT）
- 解释器 对比 编译器（对称边保留在 编译器）

## 正文链接到不存在的节点（未建 stub，仅提示）

- 无

## 其他警告

- 无

## 类型映射明细

- 属于 → 属于: 29
- 实例 → 实例: 20
- 包含 → 包含: 16
- 对比 → 对比: 12
- 部件 → 部件: 8
- 实现 → 实现: 8
- 分类 → 包含: 7
- 支撑 → 支撑: 5
- 连接 → 相关: 5
- 依赖 → 依赖: 4
- 产出 → 产出: 4
- 定义 → 控制: 4
- 划分 → 包含: 4
- 结构 → 包含: 4
- 中间 → 部件: 4
- 源自 → 源自: 3
- 被控制 → 被控制: 3
- 层次 → 包含: 3
- 约束 → 控制: 3
- 分支 → 包含: 3
- 编译目标 → 编译目标: 3
- 基于 → 基于: 3
- 相关 → 相关: 3
- 产出自 → 产出自: 3
- 目标 → 产出: 3
- 运行于 → 依赖: 3
- 物理基础 → 支撑: 2
- 实现为 → 实现: 2
- 演化为 → 演化为: 2
- 构成 → 部件: 2
- 布局 → 包含: 2
- 接入 → 依赖: 2
- 控制 → 控制: 2
- 存储 → 使用: 2
- 引出 → 演化为: 2
- 操作对象 → 使用: 2
- 操作 → 使用: 2
- 位于 → 属于: 2
- 是 → 实例: 2
- 输入到 → 使用: 2
- 配合 → 相关: 2
- 输入 → 使用: 2
- 上层于 → 基于: 2
- 编译器 → 使用: 2
- 学科 → 基于: 1
- 交换数据 → 相关: 1
- 位数决定 → 控制: 1
- 参考 → 参考: 1
- 架构 → 实例: 1
- 文档 → 参考: 1
- 扩展为 → 扩展为: 1
- 编址 → 使用: 1
- 用于 → 支撑: 1
- 被操作 → 使用: 1
- 对应学科 → 相关: 1
- 对应 → 类比: 1
- 对接 → 相关: 1
- 上层 → 基于: 1
- 内部实现 → 实现: 1
- 压力转移到 → 影响: 1
- 对外 → 实现: 1
- 对内 → 实现: 1
- 起点 → 源自: 1
- 工具 → 使用: 1
- 执行 → 使用: 1
- 翻译 → 使用: 1
- 组成 → 部件: 1
- 翻译工具 → 使用: 1
- 执行过程 → 相关: 1
- 被抽象 → 基于: 1
- 数据来源 → 使用: 1
- 调用产生 → 产出: 1
- 单位 → 部件: 1
- 由谁管理 → 被控制: 1
- 触发 → 产出: 1
- 指令 → 使用: 1
- 由谁产生 → 产出自: 1
- 待补 → 相关: 1
- 编译 → 使用: 1
- 宿主 → 依赖: 1
- 扩展 → 扩展为: 1
- 语言 → 实例: 1
- 前端 → 部件: 1
- 后端 → 部件: 1
- 衍生 → 演化为: 1
- 承载 → 支撑: 1
- 内核机制 → 包含: 1
- 类比 → 类比: 1
- 压力来自 → 影响: 1
- 抽象 → 基于: 1
